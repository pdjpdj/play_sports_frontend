import React from 'react';
import './VideoList.css';

interface VideoListProps {
}

const VideoList: React.FC<VideoListProps> = (props: VideoListProps) => {
  return (
    <div>
      This will contain the list of videos
    </div>
  );
};

export default VideoList;
